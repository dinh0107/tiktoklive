/** Soft caps — keep OBS overlay near 60fps / lower RAM. */
export const MAX_CHARACTERS = 80;
export const MAX_GIFT_QUEUE = 30;
export const MAX_PARTICLE_BURSTS = 4;
/** Max edge for character/DJ canvas textures (VRAM). */
export const CHAR_TEX_MAX = 512;
